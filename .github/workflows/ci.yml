name: CI

on:
  push:
    branches: ["**"]
  pull_request:
  workflow_dispatch:

# Least privilege by default; individual jobs/steps escalate only if/when they need to.
permissions:
  contents: read

# Cancel superseded runs on the same ref (e.g. repeated pushes to the same PR branch).
concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  PYTHON_VERSION: "3.13"

jobs:
  fast-fail:
    name: fast-fail (lint, types, tests)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7.0.0 # v7.0.0, pinned to a released tag (9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0)

      - name: Install uv
        uses: astral-sh/setup-uv@v8.2.0 # v8.2.0, pinned to a released tag (fac544c07dec837d0ccb6301d7b5580bf5edae39)
        with:
          enable-cache: true
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        run: uv sync --all-extras --dev

      - name: Lint (ruff check)
        run: uv run ruff check src tests scripts evals

      - name: Format check (ruff format)
        run: uv run ruff format --check src tests scripts evals

      - name: Type check (mypy)
        run: uv run mypy src tests

      - name: Test + coverage (pytest)
        run: uv run pytest --cov=atlas --cov-report=term-missing --cov-fail-under=80

  security:
    name: security (semgrep, bandit, pip-audit, gitleaks)
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read # gitleaks-action lists PR commits via the API on pull_request events
    steps:
      - name: Checkout
        uses: actions/checkout@v7.0.0 # v7.0.0, pinned to a released tag (9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0)
        with:
          fetch-depth: 0 # full history so gitleaks can scan all commits in the push/PR range

      - name: Install uv
        uses: astral-sh/setup-uv@v8.2.0 # v8.2.0, pinned to a released tag (fac544c07dec837d0ccb6301d7b5580bf5edae39)
        with:
          enable-cache: true
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        run: uv sync --all-extras --dev

      - name: Semgrep (p/python, p/security-audit, p/secrets)
        # SEMGREP_APP_TOKEN is optional: when unset, semgrep still runs the named community
        # rulesets locally without reporting to Semgrep AppSec Platform.
        env:
          SEMGREP_APP_TOKEN: ${{ secrets.SEMGREP_APP_TOKEN }}
        run: |
          uv run --with semgrep semgrep scan \
            --config p/python \
            --config p/security-audit \
            --config p/secrets \
            --error \
            src tests scripts evals main.py

      - name: Bandit (static security analysis)
        run: uv run bandit -r src -q

      - name: pip-audit (dependency CVE scan)
        run: uv run pip-audit

      - name: Gitleaks (secret scanning)
        uses: gitleaks/gitleaks-action@v3.0.0 # v3.0.0, pinned to a released tag (e0c47f4f8be36e29cdc102c57e68cb5cbf0e8d1e)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # Optional: only needed for Gitleaks Enterprise features. Community scanning works
          # without it.
          GITLEAKS_LICENSE: ${{ secrets.GITLEAKS_LICENSE }}

  integration:
    name: integration (postgres persistence)
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: atlas
          POSTGRES_PASSWORD: atlas
          POSTGRES_DB: atlas
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U atlas -d atlas"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql://atlas:atlas@localhost:5432/atlas
    steps:
      - name: Checkout
        uses: actions/checkout@v7.0.0 # v7.0.0, pinned to a released tag (9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0)

      - name: Install uv
        uses: astral-sh/setup-uv@v8.2.0 # v8.2.0, pinned to a released tag (fac544c07dec837d0ccb6301d7b5580bf5edae39)
        with:
          enable-cache: true
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        run: uv sync --all-extras --dev

      - name: Integration tests (Postgres-backed checkpointer + audit)
        run: uv run pytest -m integration

  agent-eval:
    name: agent-eval (security-behavior gate)
    needs: fast-fail
    # The deterministic gate is hermetic (no key, no network), so it runs broadly — on every push
    # and PR, including forks — not just PRs into main. The optional LangSmith quality evals inside
    # run_gate.py self-skip when the key is absent, so this is safe to run everywhere.
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7.0.0 # v7.0.0, pinned to a released tag (9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0)

      - name: Install uv
        uses: astral-sh/setup-uv@v8.2.0 # v8.2.0, pinned to a released tag (fac544c07dec837d0ccb6301d7b5580bf5edae39)
        with:
          enable-cache: true
          python-version: ${{ env.PYTHON_VERSION }}

      - name: Install dependencies
        run: uv sync --all-extras --dev

      - name: Run evaluation gate
        # The blocking deterministic suite needs no secrets. The keys are supplied only so the
        # optional, NON-BLOCKING LangSmith quality evals can run when available; run_gate.py never
        # echoes secret values and never lets the telemetry layer change the exit code.
        env:
          LANGSMITH_API_KEY: ${{ secrets.LANGSMITH_API_KEY }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: uv run python evals/run_gate.py
